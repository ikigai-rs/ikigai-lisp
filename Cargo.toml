[package]
name = "ikigai-lisp"
version = "0.1.8"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Steel-backed Lisp evaluator as an ikigai module: `urn:lisp:eval` runs s-expressions whose builtins ARE the kernel verbs (source/sink/meta/exists/delete), each cap-scoped and issued back through the kernel. Code is a resource; the capability is the builtin set."
authors = ["Brian Sletten <brian.sletten@gmail.com>"]
repository = "https://github.com/ikigai-rs/ikigai-lisp"
homepage = "https://github.com/ikigai-rs/ikigai-lisp"
readme = "README.md"
keywords = ["ikigai", "roc", "lisp", "scheme", "capability"]
categories = ["compilers", "caching"]

[dependencies]
# The released kernel, consumed from crates.io — the SAME version the hosts use,
# so a host links exactly one `ikigai-core` (no duplicate-crate type mismatch).
# 0.1.46 carries the typed `Error::Denied` variant a builtin surfaces on a
# capability-denied sub-request.
ikigai-core = "0.1.46"
async-trait = "0.1"
# The neutral s-expression foundation — owns the pure `sexpr_to_sparql` (query) and
# `sexpr_to_turtle` (graph) compilers over its language-agnostic `Sexpr` datum (single
# source of truth). The `(sparql-select …)` and `(graph …)` builtins adapt Steel's
# `SteelVal` into `Sexpr` and call these, rather than carrying an inline compiler.
# 0.1.1 introduced `sexpr_to_turtle`; pin 0.1.2 (the current published line).
ikigai-sexpr = "0.1.2"
# The Scheme engine; its builtins are the kernel verbs. 0.8 is the current
# embedding API (`Engine::run`, `register_fn`). A warm `new_sandboxed` template
# is built once per pooled worker and cloned per eval (~0.2 ms) — building a full
# VM per eval cost ~50 ms.
steel-core = "0.8.2"
# A Send + Sync channel pair (std::mpsc's Receiver is !Sync, which `register_fn`'s
# Send+Sync+'static bound rejects). Bridges the synchronous Steel evaluator —
# which runs on its own thread — to the async kernel driving sub-requests.
crossbeam-channel = "0.5"
# Async channel pair for the eval's SERVICING side (worker sends sync, the async
# loop awaits): the eval future must yield Pending — never block its thread — so
# Timeout overlays and schedulers can act while a program runs.
futures = "0.3"

[dev-dependencies]
# The wall-clock Timeout overlay — proves the wire-eval L1 governor end to end:
# the async servicing loop yields, so the racing timer actually fires mid-eval.
ikigai-throttle = "0.1.6"
# A real sibling module, so the `(source "urn:fn:toUpper" …)` oracle proves
# verb-as-function across a genuine module boundary, not a local stub.
ikigai-fn = "0.1.2"
# The real SPARQL engine, so the `(sparql-select …)` oracle runs a compiled query
# end-to-end through `urn:sparql:select` — dev-dep ONLY (runtime reaches it via the
# verb path, not a build link). `^0.1.5` unifies with lisp's ikigai-core (both 0.1.x),
# so one kernel type is linked across the test.
ikigai-sparql = "0.1.5"
